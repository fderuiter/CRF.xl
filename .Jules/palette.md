## 2024-05-24 - [Authoring Feedback] Learning: When managing local UI loading states for multiple actionable buttons in the same component, use a discriminated string state (e.g., `insertingType: date | ae | null`) instead of a single boolean. This prevents unrelated buttons from displaying a loading spinner simultaneously when one is clicked. Action: Remember to use discriminated state when adding async loading feedback.
