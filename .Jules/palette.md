## 2024-05-23 - [AuthoringView UX] Learning: Standalone `Excel.run()` operations for inserting smart macros in `AuthoringView` bypass global app processing states. Action: Implement explicit local React state management with `try/finally` blocks around async Office.js tasks and display local loading spinners within corresponding `<Button>` components to prevent overlapping document edits and provide user feedback.
