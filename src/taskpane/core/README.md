# Core API

This directory contains the root core taskpane logic, including main exports and configuration.

- `index` - Declares the public API boundary and exposes core types, services, validation utilities, and generators. Note: `serializeAST` is treated as an internal utility and is not part of the public exports from `index.ts`.
- `localeConfig`

